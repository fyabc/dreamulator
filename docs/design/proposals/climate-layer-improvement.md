# 气候层改进方案

> **定位**：气候引擎攻关的**设计提案**——物理第一性推理 + 文献 + 当前方案 + 已否证方向。
> 职责分工见根 CLAUDE.md「任务文档分工」：执行顺序 → `private/todos/today.md`；
> 优先级/技术债 → `docs/design/roadmap.md`；已实现管线细节 → `pipelines/climate-pipeline.md`；
> 验证方法 → `pipelines/climate-validation.md`；实验记录/历史 → git。
> **本文件不堆积实验记录，不写「旧值 → 新值」叙事。**
>
> 2026-09-11 按要素（温度 → 气压 → 风场 → 洋流 → 降水 → 分类）+ DAG 顺序重排，
> 取代原 §1–§9「攻关编号」版（其历史与实验记录在 git）。

---

## 0 定位与当前基线

**验证基线**（earth/climate-dev，200k，2026-09-11 `validate_climate`）：

| 指标 | 值 |
|---|---|
| 温度纬向 | RMSE 2.08 °C、R² 0.99、bias +1.1 °C |
| 降水纬向 | RMSE 334 mm/yr、R² 0.699、bias −199 mm/yr |
| Köppen 30 类 | 准确率 36.0%、κ 0.305 |
| Köppen 5 群 | 准确率 66.1%、κ 0.555 |

**各要素残余偏差 TOP**（2026-09-11 诊断先行盘点）：

| 要素 | 最大偏差 | 量级 | 归属 |
|---|---|---|---|
| 降水 | **海→陆输送过强** | 581 vs 观测 268 mm/yr（2.2×） | §5 大标定轮（头条） |
| 降水 | **中纬风暴路径欠输送** 38–58°N | −500~−675 mm | §5 大标定轮 |
| 降水 | ITCZ 峰值错位（8°N −441 / 8°S +417） | — | 搭 §5 观察 |
| 降水 | 南极偏干 88°S（方向性干燥过冲 663→33） | 33 vs 145 mm | 冷陷阱，与 §5 纠缠 |
| 分类 | 水分亏缺 + 热带过湿 + D 群崩溃 | ~6500 / 2658 / 4454 格 | 水分→§5；D 群→技术债 20④ |

**当前 P0 主动项**（定义见 today §〇-3 / roadmap）：§5 大标定轮 + 冷陷阱（技术债 16 并入）。

---

## 1 温度

### 当前设定

温度分两步：年平（`equilibrium_temperature` → 1D EBM / Held-Hou → 海拔递减率 → 海冰/内湖
SST）→ 季节（季节 EBM `monthly_temperature`）。

- **年平 EBM 两支**（按环流体制切换，`ebm_1d` + `hadley_extent_deg`）：
  - **三圈环流**（earth）：1D EBM（North 1975），`0 = D∇²T + Q(1−α) − (A+BT)`，D ∝ Ω^0.3
    （Kaspi & Showman 2015）；陆地 D_land = 0.6×D_total（大陆度：副热带暖、极地冷）。
  - **单圈环流**（nacrea，慢自转）：Held-Hou 四次方剖面（Held & Hou 1980），
    `T(φ) = T_mean + ΔT·(1/5 − sin⁴φ)`，`ΔT = Ω²a²θ₀/(2gH)`——副热带平、极地有冰盖，
    翻转环流输送（非涡旋扩散）。
- **副热带下沉增温**：Held-Hou 等面积均质化，把热带-副热带陆地拉向环流面积加权均值
  （翻转环流均质化，非扩散 EBM 的局地辐射平衡）。
- **海冰/内湖 SST**：冰面温度 = NCEP 锚定剖面（Semtner 1976 零层海冰热力学）；内湖 = 淡水湖
  季节热容（~10 m 上层） + 0 °C 冻结钳制。
- **季节振幅**（North & Coakley 1979）：`T_amp = ΔQ_ω(1−α)/√(B_eff² + (ωC)²)`，
  `B_eff = B_rad + 6D`（显式经向热输送，季节与年平共用 D），季节冰反照率固定点迭代。

### 残余偏差

- 大陆冬季过冷（莫斯科 −12 残、丹佛 −8）——方向性海陆温度平流已修大部分；余东亚误暖
  （哈尔滨 +12.5）是冬季风方向反（§3 / 技术债 24）。
- 沿海振幅过小（上海 7.2 vs 23.2、雅典 5.8 vs 18.8）——冬季季风（技术债 24），非本轮。

---

## 2 气压（季风强迫）

### 当前设定

海陆热力差异 → 逐月气压异常 `pressure_anomaly_monthly`（地表温度季节异常）→ 边界层风
`monsoon_boundary_layer_wind`（梯度 + 科氏 + 拖曳；科氏力符号已修正）。

### 当前方案（底稿，与技术债 24 捆绑，勿单上）

- **季风 ΔP 高地热源**：现用地表温度异常抓不到两类加热——「印度深对流加热」（地表被云雨
  冷却，季节异常仅 +4.5 K → 热低压仅 −3.9 hPa）和「高原抬升热源」（4844 m 地表温度参与，
  反而主导，与「高原是抬升热源、非地表热源」相反）。静力响应加两个海拔依赖因子：

  ```
  ΔP = −P(z) · f(z) · ΔT / T̄_zonal
  P(z) = P_sfc·exp(−z/H)          # 局地气压异常 ∝ 上方气柱质量
  f(z) = f_deep·exp(−z/z_moist)   # 水汽顶之上加热的是干空气，投影小；z_moist = 3 km
  ```

  文献：Boos & Kuang 2010（地形屏障 + 低地加热主导，非高原加热本身）、2013（非抬升加热主导）、
  Wu et al. 2012（感热驱动大气泵）。
- **已否证**：❌ f_deep 全局放大（2.8× 亚马逊 Af 崩、撒哈拉变湿）——水分收支紧平衡围绕旧
  高压共适应，须与技术债 24 捆绑再标定，单上必回退。

---

## 3 风场

### 当前设定

- 大尺度风 = 三圈环流 `hadley_cell_wind`（经向分量 + Ω^−1/3 标度，Hill 2019）+ 季风边界层风
  + 地形阻挡。
- **地转风已移除**：模型无动力学副热带高压，θ 梯度处处向极 → 地转风全纬东风、抵消 Ferrel
  西风；表面气压梯度（barometric exp(−h/H)）又带来 ~30 m/s 地形噪声。三圈环流单独给出
  连贯的 30–60° 西风 ~3–4 m/s。

### 方向性大陆度（东西不对称的落点）

- **已否证**：❌ 地转风 → 风场不对称（打崩降水 R² 0.688→0.254；副热带高压是 GCM 涌现特征，
  难用预设复刻）。
- **转向**：东西不对称体现在**温度/降水**（SotE「方向性大陆度」概念），非风场。已落地：
  ① 方向性海陆温度平流（上风向逆推海洋月温松弛）；② 雨影沿风累积（Föhn 沿整条上风路径）。

### 残余

- 风场「过于规则」（纯三圈环流，无静止行星波/副热带高压单体）——技术债 24（月度矢量场），
  非本轮。

---

## 4 洋流

### 当前设定

Stommel 正压 gyre（`solve_ocean_gyre`）+ 半拉格朗日 SST 平流 + 上升流冷却。

### 已知局限（登记，不主动攻）

- 北大西洋暖流缺失 = Stommel 正压模型边界（非 bug）。
- **半封闭海季节振幅**（地中海/波罗的海）= **GCM 级细节，EBM 级不建模**（对齐 climlab 取舍）。
  已否证「浅水→小热容」代理（这些海深于 50 m 混合层，代理错）。沿海振幅真凶是冬季季风
  （技术债 24），随 24 一并处理。

---

## 5 降水（当前头条）

### 当前设定

- **质量守恒水汽预算**（Held & Soden 2006）：`∇·(W u) + W/τ = E`，`P = W/τ`，一阶迎风
  有限体积 + κ 扩散（κ ≈ 3.75e5 m²/s），稀疏 LU 求解；Budyko 陆地再循环固定点
  （`E_land = E_pot·P/(E_pot+P)`，Savenije 1995 / van der Ent & Savenije 2011）。
- **风暴路径** = `k_rain` 空间调制（非加性源，Held & Soden 2006），`_baroclinic_band` 从
  纬向温度梯度峰值推导（Eady 不稳定性跟随 ∇T）。
- **方向性干燥** = 后乘衰减 `p *= 1 − 0.9·√(1−e^{−d/L})·g(lat)`（**过渡预设，非质量守恒**，
  纬度门控护 ITCZ）。

### 残余偏差（诊断 2026-09-11）

- **海→陆输送 2.2× 过强**：581 vs 观测 268 mm/yr（land-mean）。
- **中纬风暴路径欠输送**：storm track 陆均仅 132 mm/yr（观测中纬 ~900–1000）；纬向 38–58°N
  −500~−675 mm。
- **方向性干燥破坏 ΣP=ΣE**：P bias −199 mm/yr；南极过冲 663→33（冷陷阱被掩盖）。

### 冷陷阱（质量守恒饱和钳制）

- **症状**：冷空气柱无饱和约束照样雨出（南极原 663 vs 观测 145 mm）。
- **方案**：柱水汽饱和上限 `W ≤ W_sat(T) ≈ q_sat(T)·H_v·ρ_air/ρ_w`（Clausius-Clapeyron 饱和
  比湿 × 水汽标高，H_v ≈ 2.1 km），超额沿风向上游雨出（质量守恒）。
- **已否证**：❌ 后处理硬钳 `W = min(W, W_sat)`（破坏质量守恒，超额被钳掉非上游雨出）；
  ❌ 软饱和定点迭代 `k_sat·max(0, W−W_sat)`（大 k_sat 过刚、收敛不动）。

### 已否证方向（防重试）

- ❌ κ 空间化（输送 ~95% 是平流，κ 非主杠杆；副热带 κ 抑制是弱杠杆 O'Gorman & Schneider 2008）。

### 当前方案（P0）

收回方向性干燥（非质量守恒），换质量守恒输送；冷陷阱随收回重新暴露，**须一起处理**
（§3.2/§5 纠缠，见 §0）。

---

## 6 分类、验证与诊断

- ITCZ 峰值错位（8°N −441 / 8°S +417，南移/双 ITCZ）——搭 §5 观察。
- 陆地蒸散偏低（338 vs 490 mm/yr）——土壤湿度记忆（bucket model，干土增感热减潜热正反馈），
  待 §5 后裁决。
- D 群崩溃（Dfc→Dfb/ET 4454 格）——技术债 20④ 植被掩雪（温度×生态耦合），非本轮。
- 已否证：❌ 两季分解替代年平 BFS（ITCZ 峰削平，Af→Aw +2182）。

**诊断工具**（`scripts/climate/`）：`validate_climate`（纬向 T/P + Köppen）、
`station_diagnostics`（26 基准站逐月 T/P/风向）、`diagnose_koppen_confusion` / `_spatial`、
`diagnose_latitudinal_profile`（纬向 T/P 海陆拆分）、`diagnose_precip_budget`（输送/预算分解）、
`diagnose_pressure_anomaly`、`diagnose_monsoon_regional`、`climate_diff`（A/B 对比）。

---

## 7 已知局限 + 实现顺序

**已知局限（登记，不主动）**：

- D 群崩溃 → 技术债 20④ 植被掩雪。
- 沿海振幅（上海/雅典）→ 技术债 24 冬季季风。
- 半封闭海季节振幅 → GCM 级细节（§4）。
- 风场规则性 → 技术债 24 月度矢量场。

**实现顺序**（指向 today §〇-3 / roadmap）：§5 大标定轮 + 冷陷阱 → 技术债 16 →（后续）技术债
24（季风/风场）、技术债 20④（植被掩雪）。

---

## 参考资料

- Boos & Kuang 2010（Nature）/ 2013（Sci Rep）：南亚季风地形屏障 + 非抬升加热主导。
- Wu et al. 2012（Sci Rep）：感热驱动大气泵。
- Held & Hou 1980（JAS）：非线性轴对称环流 / Held-Hou 均质化。
- North 1975（JAS）/ North & Coakley 1979：能量平衡模型 + 季节 EBM。
- Kaspi & Showman 2015（ApJ）：慢自转经向输送标度（Ω^0.3 / Ω^−1/3）。
- Held & Soden 2006：水汽收支 `P − E = −∇·(W u)`。
- Savenije 1995 / van der Ent & Savenije 2011：陆地再循环长度。
- O'Gorman & Schneider 2008：副热带扩散抑制。
- Semtner 1976：零层海冰热力学。
- SotE/SongsOfGPL（方向性大陆度）、climlab（EBM，同架构对标）、ExoPlaSim（简化 GCM）。
